本程式沒有實作inotify監聽任何動作，僅將傳入的參數(一目錄路徑)列出其目錄內
容，列出時以字母順序排列，並且以遞迴方式列出所有子目錄內容。

輸入範例：
    ./hw6 ..

    * 範例假設你位於hw6執行檔目錄底下
    * 其後接的路徑名稱指到的對象必須要是目錄

印出格式：
    * 每印完一個檔案或目錄即換行
    * 每進入一層目錄，就會縮排4個字元寬度，第一個字元顯示"|"
